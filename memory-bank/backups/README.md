# Memory Backups

This directory stores automatic backups of the memory files created during memory operations:

- When memory files are updated (💾U)
- When project is reset (🔄R)
- When memory is reorganized (🔄O)

Backups are organized by timestamp in format `YYYY-MM-DD_HH-MM-SS`.

**Do not manually modify these files.** 