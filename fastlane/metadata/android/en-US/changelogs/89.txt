3.0.11 test release (89): legacy Xposed + official API 102, live atomic rules, batched statistics and API 102 module reload. Fix empty-regex publication, duplicate Service counts, wakelock durations, bindService returns and accidental statistics clearing. Back up rules; first migration may need a reboot. OP13/Vector 2.2 tested on the preceding candidate; final legacy-device and other boundary tests remain. Full limits: GitHub release notes.
